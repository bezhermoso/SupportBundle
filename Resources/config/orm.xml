<?xml version="1.0" ?>

<container xmlns="http://symfony.com/schema/dic/services"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://symfony.com/schema/dic/services http://symfony.com/schema/dic/services/services-1.0.xsd">

    <!--
    <parameters>
        <parameter key="bez_support.example.class">Bez\SupportBundle\Example</parameter>
    </parameters>

    <services>
        <service id="bez_support.example" class="%bez_support.example.class%">
            <argument type="service" id="service_id" />
            <argument>plain_value</argument>
            <argument>%parameter_name%</argument>
        </service>
    </services>
    -->
    <parameters>
       <parameter key="bez_support.ticket_manager.class">Bez\SupportBundle\Doctrine\TicketManager</parameter>
       <parameter key="bez_support.orm.entity_manager">default</parameter>
       <parameter key="bez_support.orm.ticket_class" type="constant">NULL</parameter>
       <parameter key="bez_support.orm.comment_class" type="constant">NULL</parameter>
    </parameters>
    <services>

        <service id="bez_support.orm.entity_manager"
                 class="Doctrine\ORM\EntityManager"
                 factory-service="doctrine" factory-method="getManager">
           <argument>%bez_support.orm.entity_manager%</argument>
         </service>

        <service id="bez_support.ticket_manager" class="%bez_support.ticket_manager.class%">
            <argument type="service" id="bez_support.orm.entity_manager" />
            <argument>%bez_support.orm.ticket_class%</argument>
        </service>
    </services>
</container>
